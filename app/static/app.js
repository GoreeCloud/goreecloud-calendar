const state={cursor:new Date(),events:[]};
const grid=document.querySelector('#grid');const title=document.querySelector('#monthTitle');const statusEl=document.querySelector('#status');const agenda=document.querySelector('#agendaList');const theme=document.querySelector('#theme');
function isoDate(d){return d.toISOString().slice(0,10)}
function monthBounds(d){const start=new Date(d.getFullYear(),d.getMonth(),1);const end=new Date(d.getFullYear(),d.getMonth()+1,1);return{start,end}}
function eventDate(e){return new Date(e.start)}
function render(){const{start}=monthBounds(state.cursor);title.textContent=start.toLocaleDateString(undefined,{month:'long',year:'numeric'});grid.replaceChildren();const first=new Date(start);first.setDate(1-first.getDay());for(let i=0;i<42;i++){const day=new Date(first);day.setDate(first.getDate()+i);const cell=document.createElement('div');cell.className='day';cell.tabIndex=0;if(day.getMonth()!==start.getMonth())cell.classList.add('outside');const now=new Date();if(day.toDateString()===now.toDateString())cell.classList.add('today');const num=document.createElement('span');num.className='day-number';num.textContent=day.getDate();cell.append(num);state.events.filter(e=>eventDate(e).toDateString()===day.toDateString()).slice(0,3).forEach(e=>{const chip=document.createElement('span');chip.className='event-chip';chip.textContent=e.summary||'Untitled event';chip.title=chip.textContent;cell.append(chip)});grid.append(cell)}renderAgenda()}
function renderAgenda(){agenda.replaceChildren();const ordered=[...state.events].sort((a,b)=>eventDate(a)-eventDate(b));if(!ordered.length){const p=document.createElement('p');p.className='empty';p.textContent='No events in this range.';agenda.append(p);return}ordered.slice(0,20).forEach(e=>{const item=document.createElement('article');item.className='agenda-item';const when=document.createElement('time');const d=eventDate(e);when.dateTime=e.start;when.textContent=d.toLocaleDateString(undefined,{month:'short',day:'numeric'});const body=document.createElement('div');const strong=document.createElement('strong');strong.textContent=e.summary||'Untitled event';body.append(strong);if(e.location){const p=document.createElement('p');p.textContent=e.location;body.append(p)}item.append(when,body);agenda.append(item)})}
async function load(){const{start,end}=monthBounds(state.cursor);statusEl.textContent='Loading events…';try{const res=await fetch(`/api/events?start=${isoDate(start)}&end=${isoDate(end)}`,{headers:{Accept:'application/json'}});const body=await res.json();if(!res.ok)throw new Error(body.detail||'Calendar service unavailable');state.events=body.events||[];statusEl.textContent=body.readOnly?'Read-only CalDAV view':'Calendar loaded';render()}catch(err){state.events=[];statusEl.textContent=err.message;render()}}
document.querySelector('#previous').addEventListener('click',()=>{state.cursor=new Date(state.cursor.getFullYear(),state.cursor.getMonth()-1,1);load()});document.querySelector('#next').addEventListener('click',()=>{state.cursor=new Date(state.cursor.getFullYear(),state.cursor.getMonth()+1,1);load()});document.querySelector('#today').addEventListener('click',()=>{state.cursor=new Date();load()});theme.value=localStorage.getItem('goreecloud-calendar-theme')||'system';theme.addEventListener('change',()=>{localStorage.setItem('goreecloud-calendar-theme',theme.value);document.documentElement.dataset.theme=theme.value});load();
